<% if user_signed_in? %>
	<% if current_user.valid_token? %>	
		<!-- display main page -->
		<%= render 'moves_main' %>
	<% else %>
		<!-- link your account to moves -->
		<%= render 'link_account' %>
	<% end %>
<% else %>
	<!-- landing page, sign in/up -->
	<%= render 'landing' %>
<% end %>
